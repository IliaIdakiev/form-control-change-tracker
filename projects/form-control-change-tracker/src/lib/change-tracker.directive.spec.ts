import { ChangeTrackerDirective } from './change-tracker.directive';

describe('ChangeTrackerDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeTrackerDirective();
    expect(directive).toBeTruthy();
  });
});
